# 默认演示数据保存在浏览器，与真实 API 数据隔离。
VITE_DATA_MODE=demo
VITE_API_BASE_URL=/api/v1
# 此变量仅由 Vite 开发服务器读取，不打包进浏览器。
API_PROXY_TARGET=http://127.0.0.1:8000

# 高德浏览器端专用 Key，不能填写后端 MCP Key。
VITE_AMAP_JS_KEY=
# 正式环境优先配置高德安全代理地址。
VITE_AMAP_SECURITY_PROXY=
# 仅供本地开发的高德安全密钥；VITE_ 变量对浏览器可见。
VITE_AMAP_SECURITY_CODE=
